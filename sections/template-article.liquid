<h1>{{ article.title }}</h1>
<small>{{ article.author }}</small>
<time>{{ article.updated_at | date: format: 'long' }}</time>
<br>
<img src="{{ article.image.src | img_url: 'medium' }}" alt="{{ article.image.alt }}">
<p>{{ article.content }}</p>